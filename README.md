
  # turf

  This is a code bundle for turf. The original project is available at https://www.figma.com/design/59bM8arbklTgv4GiDcAzZz/turf.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  